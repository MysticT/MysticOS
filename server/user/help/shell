Functions in the Shell Lib:
- shell.run(cmd, args)
	* runs a program with the given arguments
	* return values:
		- boolean indicating if the program executed correctly (boolean)
		- any values returned by the program, on error the string with the error message
	* parameters:
		- cmd: the command to run (program name or alias) (string)
		- args: the arguments passed to the program
- shell.runService(cmd, args)
	* runs a program on the background with the given arguments
	* return values:
		- boolean indicating if the program started (boolean)
		- string containing the error message, if any (string, nil)
	* parameters:
		- cmd: the command to run (program name or alias) (string)
		- args: the arguments passed to the program
- shell.logout()
	* logs out the current user and returns to the login screen
- shell.exit()
	* exits the shell
- shell.shutdown()
	* shuts down the computer
- shell.reboot()
	* reboots the computer
- shell.dir()
	* gets the current directory
	* return values:
		- the current directory (string)
- shell.setDir(dir)
	* changes the current directory
	* parameters:
		- dir: the directory to set (string)
- shell.resolve(path)
	* resolves the given path from the current directory
	* return values:
		- the resolved path (string)
	* parameters:
		- path: the path to resolve (string)
- shell.resolveProgram(cmd)
	* resolves the given command to the associated program
	* return values:
		- the resolved path to the program (string)
	* parameters:
		- cmd: the program or alias to resolve (string)
- shell.programs(includeHidden)
	* gets a list of available programs
	* return values:
		- the list of programs (table)
	* parameters:
		- includeHidden: true to include hidden programs (boolean) [optional]
- shell.getRunningProgram()
	* gets the path to the running program
	* return values:
		- the path to the running program (string)
- shell.setAlias(cmd, program)
	* sets an alias for the given program
	* parameters:
		- cmd: the alias for the program (string)
		- program: the program to set the alias (string)
- shell.clearAlias(cmd)
	* removes the given alias
	* parameters:
		- cmd: the alias to remove (string)
- shell.aliases()
	* gets a list of all the current aliases
	* return values:
		- the list of aliases (table)