local tArgs = { ... }
if #tArgs < 1 then
	print("Usage: rm <path>")
	return
end

local sPath = shell.resolve(tArgs[1])
local ok, nDirs, nFiles = fs.delete(sPath)
if ok then
	print("Directories deleted: ", nDirs)
	print("Files deleted: ", nFiles)
else
	print(nDirs)
end