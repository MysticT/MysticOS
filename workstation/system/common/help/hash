Functions in the Hash Lib:
- md5(s)
	* calculate the md5 hash of the given string
	* return values:
		- the md5 hash (string)
	* parameters:
		- s: the string to calculate the hash (string)
- sha256(s)
	* calculate the sha256 hash of the given string
	* return values:
		- the sha256 hash (string)
	* parameters:
		- s: the string to calculate the hash (string)
- sha224(s)
	* calculate the sha224 hash of the given string
	* return values:
		- the sha224 hash (string)
	* parameters:
		- s: the string to calculate the hash (string)