math is a standard Lua5.1 Lib.
Refer to http://www.lua.org/manual/5.1/ for more information.

Classes in the Math Lib:
- Matrix
	- new(R, C)
		* create a new matrix of RxC
		* return values:
			- the new matrix (Matrix)
		* parameters:
			- R: the number of rows (number)
			- C: the number of columns (number)
	- get(row, col)
		* get a value from the matrix
		* return values:
			- the value (number)
		* parameters:
			- row: the row number (number)
			- col: the column number (number)
	- set(row, col, val)
		* set a value on the matrix
		* parameters:
			- row: the row number (number)
			- col: the column number (number)
			- val: the value to set (number)
	- multiply(m)
		* multiply this matrix by another one
		* return values:
			- the resulting matrix (Matrix)
		* parameters:
			- m: the matrix to multiply (Matrix)
	- transpose()
		* transpose the matrix
		* return values:
			- the transposed matrix (Matrix)
- Vector2D
	- new(x, y)
		* create a new 2d vector
		* return values:
			- the new vector (Vector2D)
		* parameters:
			- x, y: the values of the vector (number)
	- set(x, y)
		* set the values of the vector
		* parameters:
			- x, y: the new values of the vector (number)
	- lenght()
		* gets the lenght of this vector
		* return values:
			- the lenght of the vector (number)
	- normalize()
		* normalizes the vector
- Vector3D
	- new(x, y, z)
		* create a new 3d vector
		* return values:
			- the new vector (Vector3D)
		* parameters:
			- x, y, z: the values of the vector (number)
	- set(x, y, z)
		* set the values of the vector
		* parameters:
			- x, y, z: the new values of the vector (number)
	- lenght()
		* gets the lenght of this vector
		* return values:
			- the lenght of the vector (number)
	- normalize()
		* normalizes the vector
	- cross_product(v)
		* get the cross product of this vector and other one
		* return values:
			- the resulting vector (Vector3D)
		* parameters:
			- v: the vector to multiply (Vector3D)