table is a standard Lua5.1 Lib with some functions added.
Refer to http://www.lua.org/manual/5.1/ for more information.

Functions added:
- table.contains(table, value)
	* checks if the table contains the given value
	* return values:
		- boolean indicating if the table contains the value (boolean)
	* parameters:
		- table: the table to check (table)
		- value: the value to search
- table.contains_key(table, key)
	* checks if the table contains the given key
	* return values:
		- boolean indicating if the table contains the key (boolean)
	* parameters:
		- table: the table to check (table)
		- key: the key to search
- table.clear(table)
	* removes every key and value from the table
	* parameters:
		- table: the table to clear (table)
- table.copy(table)
	* makes a copy of the table
	* return values:
		- the copied table (table)
	* parameters:
		- table: the table to copy (table)
- table.keys(table)
	* gets a list of every key on the table
	* return values:
		- the list of keys (table)
	* parameters:
		- table: the table to get it's keys (table)
- table.values(table)
	* gets a list of every value on the table
	* return values:
		- the list of values (table)
	* parameters:
		- table: the table to get it's values (table)