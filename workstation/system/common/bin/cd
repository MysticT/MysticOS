local tArgs = { ... }
if #tArgs ~= 1 then
	print("Usage: cd <path>")
	return
end

local sNewDir = shell.resolve(tArgs[1])
local bLink, sLinkPath = fs.isLink(sNewDir)
if bLink then
	sNewDir = sLinkPath
end

if fs.isDir(sNewDir) then
	shell.setDir(sNewDir)
else
  	print("Not a directory")
end