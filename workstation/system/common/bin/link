local tArgs = { ... }
if #tArgs ~= 2 then
	print("Usage: link <path> <destination>")
	return
end

local ok, err = fs.createLink(tArgs[1], tArgs[2])
if not ok then
	print(err)
else
	print("Link created")
end