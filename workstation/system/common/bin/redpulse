local tArgs = { ... }
if #tArgs < 1 or #tArgs > 3 then
	print("Usage: redpulse <side> <count> <period>")
	return
end

local sSide = tArgs[1]
local nCount = tonumber(tArgs[2]) or 1
local nPeriod = tonumber(tArgs[3]) or 0.5

for n = 1, nCount do
	rs.pulse(sSide, nPeriod)
end